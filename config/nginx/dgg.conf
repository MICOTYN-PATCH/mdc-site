index index.html index.php;

server {
    listen 8080;
    server_name micotyn.com;
    rewrite ^(.*) "https://www.micotyn.com$1" permanent;
}

server {
    listen 8080;
    server_name stage.micotyn.com;
    root /www/stage.micotyn.com/public;

    location ~ ^/\. { deny  all; }
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|map)$ {
        expires 365d;
    }

    location / {
        try_files $uri $uri/ @phprewrite;
    }

    location @phprewrite {
        rewrite ^/.*$ /index.php;
    }

    location ~ \.php$ {
        if ($request_uri !~ "^/embed") {
            add_header X-Frame-Options "SAMEORIGIN";
            add_header X-XSS-Protection "1; mode=block";
            add_header Content-Security-Policy "default-src *; script-src 'unsafe-inline' *.micotyn.com https://apis.google.com www.google.com www.google-analytics.com https://ssl.google-analytics.com https://*.googlesyndication.com https://googleads.g.doubleclick.net https://*.gstatic.com; object-src 'none'; style-src 'self' data: 'unsafe-inline' *.micotyn.com fonts.googleapis.com; img-src 'self' data: *.micotyn.com www.google-analytics.com https://ssl.google-analytics.com https://ting.7eer.net https://adn-ssl.impactradius.com https://ssl.gstatic.com; media-src 'none'; frame-src 'self' https://www.google.com www.twitch.tv player.twitch.tv clips.twitch.tv googleads.g.doubleclick.net; font-src *.micotyn.com fonts.googleapis.com fonts.gstatic.com; connect-src *";
        }
        add_header X-XSS-Protection "1; mode=block";
        add_header Strict-Transport-Security "max-age=2700000; includeSubDomains; preload";
        include dgg_csp.conf;

        fastcgi_pass  127.0.0.1:850;
        fastcgi_index index.php;
        include       fastcgi.conf;
    }
}

server {
    listen 8080;
    server_name stagecdn.micotyn.com;
    root /www/stage.micotyn.com/static;

    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=2700000; includeSubDomains; preload";
    include dgg_csp.conf;

    add_header "Access-Control-Allow-Origin" "https://stage.micotyn.com";
    add_header "Access-Control-Allow-Methods" "GET, POST, OPTIONS";

    rewrite ^/\d+\.\d+\.\d+/(.*)$ /$1;
    location ~ ^/\. { deny  all; }
    location ~* \.(eot|jpe?g|png|gif|ico|css|js|map|svg|ttf|woff2?)$ {
        expires 365d;
    }

    location / {
        rewrite .* "https://stage.micotyn.com" permanent;
    }
}

server {
    listen 8080;
    server_name cdn.micotyn.com;
    root /www/www.micotyn.com/static;

    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=2700000; includeSubDomains; preload";
    include dgg_csp.conf;

    add_header "Access-Control-Allow-Origin" "https://www.micotyn.com";
    add_header "Access-Control-Allow-Methods" "GET, POST, OPTIONS";

    rewrite ^/\d+\.\d+\.\d+/(.*)$ /$1;
    location ~ ^/\. { deny  all; }
    location ~* \.(eot|jpe?g|png|gif|ico|css|js|map|svg|ttf|woff2?)$ {
        expires 365d;
    }

    location / {
        rewrite .* "https://www.micotyn.com" permanent;
    }
}

server {
    listen 8080;
    server_name blog.micotyn.com;
    root /www/blog.micotyn.com;

    location ~ ^/\. { deny  all; }
    location / {
        try_files $uri $uri/ @wprewrite;
    }

    location @wprewrite {
        rewrite ^(.+)$ /index.php?q=$1 last;
    }

    location ~ \.php$ {
        if ($request_uri ~ ^/wp-admin/) {
            add_header Content-Security-Policy "default-src *; script-src 'unsafe-inline' 'unsafe-eval' *.micotyn.com; object-src 'none'; style-src 'self' data: 'unsafe-inline' *.micotyn.com fonts.googleapis.com; img-src 'self' data: *.micotyn.com https://secure.gravatar.com; media-src 'none'; frame-src 'self'; font-src data: *.micotyn.com fonts.googleapis.com fonts.gstatic.com; connect-src *";
        }
        include dgg_csp.conf;
        fastcgi_pass  127.0.0.1:850;
        fastcgi_index index.php;
        include       fastcgi.conf;
    }
}

server {
    listen 8080;
    server_name www.micotyn.com 127.0.0.1;
    root /www/www.micotyn.com/public;

    location ~ ^/\. { deny  all; }
    location /n/ {
        rewrite ^(.*) "https://blog.micotyn.com/$1" permanent;
    }
    location /n {
        rewrite .* "https://blog.micotyn.com/" permanent;
    }

    location ~* \.(jpg|jpeg|png|gif|ico|css|js|map|svg)$ {
        expires 365d;
    }

    location / {
        try_files $uri $uri/ @phprewrite;
    }

    location @phprewrite {
        rewrite ^/.*$ /index.php;
    }

    location ~ \.php$ {
        if ($request_uri !~ "^/embed") {
            add_header X-Frame-Options "SAMEORIGIN";
            add_header X-XSS-Protection "1; mode=block";
            add_header Content-Security-Policy "default-src *; script-src 'unsafe-inline' *.micotyn.com https://apis.google.com www.google.com www.google-analytics.com https://ssl.google-analytics.com https://*.googlesyndication.com https://googleads.g.doubleclick.net https://*.gstatic.com; object-src 'none'; style-src 'self' data: 'unsafe-inline' *.micotyn.com fonts.googleapis.com; img-src 'self' data: *.micotyn.com www.google-analytics.com https://ssl.google-analytics.com https://ting.7eer.net https://adn-ssl.impactradius.com https://ssl.gstatic.com; media-src 'none'; frame-src 'self' https://www.google.com www.twitch.tv clips.twitch.tv player.twitch.tv googleads.g.doubleclick.net; font-src *.micotyn.com fonts.googleapis.com fonts.gstatic.com; connect-src *";
        }

        add_header X-XSS-Protection "1; mode=block";
        add_header Strict-Transport-Security "max-age=2700000; includeSubDomains; preload";
        include dgg_csp.conf;

        fastcgi_pass  127.0.0.1:850;
        fastcgi_index index.php;
        include       fastcgi.conf;
    }
}

server {
    listen 8080;
    server_name devcdn.micotyn.gg;
    root /www/dev.micotyn.gg/static;

    auth_basic "DA DEV SITEZ";
    auth_basic_user_file /www/htpasswd;

    add_header X-XSS-Protection "1; mode=block";
    include dgg_csp.conf;

    add_header "Access-Control-Allow-Origin" "https://dev.micotyn.com";
    add_header "Access-Control-Allow-Methods" "GET, POST, OPTIONS";

    rewrite ^/\d+\.\d+\.\d+/(.*)$ /$1;
    location ~ ^/\. { deny  all; }
    location ~* \.(eot|jpe?g|png|gif|ico|css|js|map|svg|ttf|woff2?)$ {
        expires 365d;
    }

    location / {
        rewrite .* "https://dev.micotyn.com" permanent;
    }
}

server {
    listen 8080;
    server_name dev.micotyn.com;
    root /www/dev.micotyn.com/public;

    satisfy any;
    allow 127.0.0.1;
    #allow ::1; # some nginx versions do not support ipv6 localhost
    deny  all;
    auth_basic "DA DEV SITEZ";
    auth_basic_user_file /www/htpasswd;

    location ~ ^/\. { deny  all; }
    location / {
        try_files $uri $uri/ @phprewrite;
    }

    location /nginx_status {
        stub_status on;
    }

    location @phprewrite {
        rewrite .* /index.php;
    }

    location ~ \.php$ {
        if ($request_uri !~ "^/embed") {
            add_header X-Frame-Options "SAMEORIGIN";
            add_header X-XSS-Protection "1; mode=block";
            add_header Content-Security-Policy "default-src *; script-src 'unsafe-inline' *.micotyn.com https://apis.google.com www.google.com www.google-analytics.com https://ssl.google-analytics.com https://*.googlesyndication.com https://googleads.g.doubleclick.net https://*.gstatic.com; object-src 'none'; style-src 'self' data: 'unsafe-inline' *.micotyn.com fonts.googleapis.com; img-src 'self' data: *.micotyn.com www.google-analytics.com https://ssl.google-analytics.com https://ting.7eer.net https://adn-ssl.impactradius.com https://ssl.gstatic.com; media-src 'none'; frame-src 'self' https://www.google.com www.twitch.tv clips.twitch.tv player.twitch.tv googleads.g.doubleclick.net; font-src *.micotyn.com fonts.googleapis.com fonts.gstatic.com; connect-src *";
        }

        add_header X-XSS-Protection "1; mode=block";
        include dgg_csp.conf;

        fastcgi_pass  127.0.0.1:850;
        fastcgi_index index.php;
        include       fastcgi.conf;
    }
}

server {
    listen 8080;
    server_name sql.micotyn.com;
    root /www/sql.micotyn.com;

    auth_basic "DA DATABAZE SITEZ";
    auth_basic_user_file /www/htpasswd;
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=2700000; preload";

    location ~ ^/\. { deny  all; }
    location ~ \.php$ {
        fastcgi_pass  127.0.0.1:851;
        fastcgi_index index.php;
        include       fastcgi.conf;
    }
}
